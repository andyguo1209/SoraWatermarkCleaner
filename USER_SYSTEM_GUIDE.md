# 用户体系使用指南

## 概述

本系统已添加完整的用户认证和历史记录功能，每个用户可以独立管理自己的视频处理任务。

## 新增功能

### 1. 用户认证系统

#### 用户注册
- 支持用户名、密码和可选邮箱注册
- 用户名：3-50个字符
- 密码：至少6个字符
- 密码采用 bcrypt 加密存储，确保安全性

#### 用户登录
- 使用用户名和密码登录
- 登录成功后获取访问令牌
- 令牌有效期：7天

### 2. 历史记录功能

#### 任务追踪
- 每个用户的视频处理任务独立记录
- 记录内容包括：
  - 视频文件名
  - 任务状态（上传中、处理中、已完成、失败）
  - 处理进度（百分比）
  - 创建时间
  - 更新时间
  - 错误信息（如果失败）

#### 历史记录查看
- 统计面板显示：
  - 已完成任务数
  - 处理中任务数
  - 失败任务数
  - 总任务数
- 任务列表展示：
  - 按创建时间倒序排列
  - 显示任务详细信息和进度
  - 已完成任务可直接下载

### 3. 用户界面改进

#### 导航栏
- 首页：返回视频上传页面
- 历史记录：查看所有处理记录
- 用户信息：显示当前登录用户
- 退出登录：退出当前账户

#### 页面保护
- 未登录用户自动跳转到登录页
- 所有API接口需要认证才能访问
- 任务和用户数据完全隔离

## 技术架构

### 数据库模型

#### User 表
```python
- id: 用户ID（主键）
- username: 用户名（唯一）
- password_hash: 密码哈希
- email: 邮箱（可选，唯一）
- created_at: 注册时间
- last_login: 最后登录时间
```

#### Task 表
```python
- id: 任务ID（主键）
- user_id: 用户ID（外键）
- video_path: 视频路径
- video_filename: 视频文件名
- output_path: 输出路径
- status: 任务状态
- percentage: 处理进度
- download_url: 下载链接
- error_message: 错误信息
- created_at: 创建时间
- updated_at: 更新时间
```

### API 端点

#### 用户相关
- `POST /register` - 用户注册
- `POST /login` - 用户登录
- `GET /me` - 获取当前用户信息
- `GET /history` - 获取用户历史记录

#### 任务相关
- `POST /submit_remove_task` - 提交视频处理任务
- `GET /get_results` - 获取任务结果
- `GET /download/{task_id}` - 下载处理后的视频

### 安全措施

1. **密码加密**
   - 使用 bcrypt 算法加密密码
   - 密码不以明文形式存储

2. **令牌认证**
   - 使用 JWT 技术生成访问令牌
   - 所有受保护的API需要在请求头中携带令牌

3. **数据隔离**
   - 每个用户只能访问自己的数据
   - API层面进行用户ID验证

4. **输入验证**
   - 使用 Pydantic 进行数据验证
   - 防止SQL注入和XSS攻击

## 使用流程

### 首次使用

1. 启动后端服务器
```bash
python start_server.py
```

2. 启动前端应用
```bash
streamlit run app.py
```

3. 在浏览器中打开应用
4. 点击"注册"标签页
5. 填写用户信息并注册
6. 切换到"登录"标签页
7. 使用注册的账号登录

### 日常使用

1. 使用用户名和密码登录
2. 上传需要处理的视频
3. 等待AI处理完成
4. 下载处理后的视频
5. 在"历史记录"页面查看所有处理记录

## 配置说明

### API基础URL配置

在 `app.py` 中可以修改 API 基础URL：

```python
API_BASE_URL = "http://localhost:8000"
```

### 数据库配置

数据库文件位于 `working_dir/tasks.db`（由 `sorawm/configs.py` 中的 `SQLITE_PATH` 配置）

### 令牌有效期配置

在 `sorawm/server/auth_utils.py` 中修改：

```python
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7  # 7天
```

## 注意事项

1. **首次运行**
   - 首次运行时会自动创建数据库表
   - 需要先启动后端服务器再启动前端

2. **密码安全**
   - 请使用强密码
   - 不要与他人共享账号

3. **数据备份**
   - 定期备份数据库文件
   - 重要视频请及时下载保存

4. **性能考虑**
   - 历史记录默认显示最近50条
   - 可在 API 调用时调整 limit 参数

## 故障排查

### 登录失败
- 检查用户名和密码是否正确
- 确认后端服务器是否正常运行
- 检查 API_BASE_URL 配置是否正确

### 历史记录无法加载
- 检查网络连接
- 确认令牌是否有效（重新登录）
- 查看浏览器控制台错误信息

### 视频下载失败
- 检查任务是否已完成
- 确认输出文件是否存在
- 检查磁盘空间是否充足

## 未来改进方向

1. 添加密码重置功能
2. 支持用户头像上传
3. 添加任务统计图表
4. 支持批量任务管理
5. 添加任务优先级设置
6. 支持任务分享功能

## 技术支持

如有问题，请访问：
- GitHub: https://github.com/linkedlist771/SoraWatermarkCleaner
- 提交 Issue 获取帮助

